import { VowelByNumberPipe } from './vowel-by-number.pipe';

describe('VowelByNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VowelByNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
